{
  "name": "genai-chat-to-md-ext",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "prepare": "lefthook install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild background.ts content.ts --bundle --outdir=dist --format=esm --platform=browser",
    "watch": "esbuild background.ts content.ts --bundle --outdir=dist --format=esm --platform=browser --watch",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "format": "biome format --write .",
    "lint": "biome lint ."
  },
  "dependencies": {
    "turndown": "^7.2.2",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.10",
    "@types/chrome": "0.1.38",
    "@types/turndown": "5.0.6",
    "esbuild": "0.27.4",
    "lefthook": "2.1.4"
  }
}
